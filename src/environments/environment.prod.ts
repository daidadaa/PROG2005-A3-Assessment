// Angular Environment Configuration — Production
// prod env config
// Author: WU Shaowei — production API endpoint (same as dev)

export const environment = {
  production: true,
  apiUrl: 'https://prog2005.it.scu.edu.au/ArtGalley'
};
